[tool.uv.workspace]
members = ["axiom-*", "oltp/axiom-*", "olap/axiom-*"]

[dependency-groups]
dev = [
    "mypy>=1.16.0,<2",
    "pip-audit>=2.9.0,<3",
    "pre-commit>=4.2.0,<5",
    "pylint>=3.3.0,<4",
    "ruff>=0.11.13,<=0.12.0",
    "vulture>=2.15",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
filterwarnings = [
    "ignore:.*uvicorn.workers.*:DeprecationWarning",
    "ignore:.*wait_for_logs.*:DeprecationWarning",
    "ignore:.*wait_container_is_ready.*:DeprecationWarning",
]
markers = [
    "slow: tests requiring external services (Docker/testcontainers) — run with -m slow",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    "W",
    # pyflakes
    "F",
    # pydocstyle
    "D",
    # isort
    "I",
    # tidy imports
    "TID",
    # flake8-bugbear
    "B",
    # flake8-bandit
    "S",
    # pyugrade
    "UP",
]
ignore = ["B008", "E721"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.pycodestyle]
max-doc-length = 120
max-line-length = 120

[tool.ruff.lint.per-file-ignores]
"*/tests/*" = ["S101", "D"]

[tool.ruff.lint.isort]
known-first-party = ["axiom"]

[tool.ruff.format]
docstring-code-format = true

[tool.mypy]

[[tool.mypy.overrides]]
module = "aiofiles.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "uvloop.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "gunicorn.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "clickhouse_connect.*"
ignore_missing_imports = true
